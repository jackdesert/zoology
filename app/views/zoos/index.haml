%p{id: 'notice'}= notice

%h1 All Our Zoos

%table
  %thead
    %tr
      %th Name
      %th{colspan: 3}

  %tbody
    - @zoos.each do |zoo|
      %tr
        %td
          %a{href: zoo_path(zoo)}= zoo.name
        /%td= link_to 'Edit', edit_zoo_path(zoo)
        /%td= link_to 'Destroy', zoo, method: :delete, data: { confirm: 'Are you sure?' }
%br

= link_to 'Add a Zoo', new_zoo_path
